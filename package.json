
{
  "name": "flare-crosschain-dex",
  "version": "1.0.0",
  "description": "Cross-chain DEX on Flare Network with 1inch integration",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:flare": "hardhat run scripts/deploy.js --network flare",
    "deploy:coston2": "hardhat run scripts/deploy.js --network coston2",
    "verify": "hardhat verify",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.0"
  },
  "dependencies": {
    "@flarenetwork/flare-periphery-contracts": "^1.0.0"
  }
}
